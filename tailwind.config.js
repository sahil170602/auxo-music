// If you are using Tailwind CSS v4 with the @import syntax, 
// this file is typically not needed or can be left minimal.
// The configuration is handled directly in your CSS file.

export default {
  // Config is now optional in v4 for basic usage
}